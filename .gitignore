.env
.env.local
secrets/
node_modules/
__pycache__/
*.pyc
.DS_Store

# training raw data (reproducible via ml_credit/data.py)
ml_credit/data/

# live database + local backups: never commit data or borrower PII.
# (Seed/artifact DBs needed for deploy ship explicitly, not via this rule.)
backend/lending.db
backend/lending.db-wal
backend/lending.db-shm
backend/lending.db-journal
backend/backups/

# fairness-ablation experiment artifacts (comparison recorded in credit_v4/metadata.json)
backend/models/credit_v4_nosensitive/
