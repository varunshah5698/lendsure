# Vercel serverless runtime deps (slim on purpose — 250 MB function limit).
# Same stack as backend/requirements.txt minus training-only extras
# (xgboost/lightgbm/shap are used by train.py and optional SHAP explainability;
# serving + fallbacks work without them). Local dev still uses
# backend/requirements.txt unchanged.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
python-multipart>=0.0.9
# Password hashing: Argon2id (PBKDF2 fallback if absent).
argon2-cffi>=23.1
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3
